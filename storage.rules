rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write for everyone (for development)
      // In production, you should restrict this to authenticated users
      allow read, write: if true;
    }
  }
}
